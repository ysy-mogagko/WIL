# 2026-06-20 진행 내용
## neovim 사용 시 json fold 기능 설정
### 환경 설정
```
brew install tree-sitter-cli

:TSInstall json jsonc
```
### config
```
# lua/autocmds/fold.lua
local group = vim.api.nvim_create_augroup("JsonFolding", {
  clear = true,
})

vim.api.nvim_create_autocmd("FileType", {
  group = group,
  pattern = { "json", "jsonc" },
  callback = function()
    vim.opt_local.foldmethod = "expr"
    vim.opt_local.foldexpr = "v:lua.vim.treesitter.foldexpr()"
    vim.opt_local.foldenable = true

    -- 파일을 열었을 때 전체가 접히지 않도록 설정
    vim.opt_local.foldlevel = 99
    vim.opt_local.foldlevelstart = 99
  end,
})


# lua/plugins/treesitter
local parsers = {
  "json",
  "jsonc",
}

return {
  {
    "nvim-treesitter/nvim-treesitter",
    branch = "main",
    lazy = false,

    build = function()
      require("nvim-treesitter").install(parsers):wait(300000)
    end,
  },
}
```
## 개인 프로젝트 토스 API 연동 전 service 간 pipeline 구축
- 기존 notice 내용을 telegram으로 분리
- internal/hub 생성
 - types 정의
